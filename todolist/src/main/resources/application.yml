spring:
  datasource:
    url: jdbc:mysql://localhost:3306/feature?useSSL=false&allowPublicKeyRetrieval=true
    username: root
    password: 
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        #        show_sql: true System.out? ??
        format_sql: true

logging:
  level:
    org.hibernate.SQL: debug # log? ?? ? show_sql? ???? ?? ?? ????.
#    org.hibernate.type: trace # ?? ????? ????.